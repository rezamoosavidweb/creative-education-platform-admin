# Base URL of the backend API (NestJS). No trailing slash, no version prefix.
VITE_API_URL=http://localhost:3000

# Optional: only needed for the separate Clerk demo auth tree (routes/clerk/*)
VITE_CLERK_PUBLISHABLE_KEY=
